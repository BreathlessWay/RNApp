import { StyleSheet } from 'react-native';

const Style = StyleSheet.create({
	right: {
		flexDirection: 'row',
		alignItems: 'center',
	},
	star: {
		marginHorizontal: 10,
	},
});

export default Style;
